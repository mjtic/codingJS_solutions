function makeOutWord(out,word){
return out.substring(0,2)+word+out.substring(2);
}


/*
function makeOutWord(out,word){
return `${out.substring(0,2)}${word}${out.substring(2)}`
}
*/

/*
function makeOutWord(out, word) {
 const middle = out.length/2;
 const front = out.substring(0, middle);
 const back = out.substring(middle);
  return front+word+back;
}
*/