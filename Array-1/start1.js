function start1(a, b) {
  return ((a.length > 0 && a[0] === 1) ? 1 : 0) + ((b.length > 0 && b[0] === 1) ? 1 : 0);
}

/*
function start1(a, b){
  let count = 0;
  if(a.length>0 && a[0]===1){
    count++;
  }
  if(b.length>0 && b[0]===1){
    count++;
  }
  return count;
}
*/